import { Component, ViewEncapsulation } from "@angular/core";

@Component({
    selector: 'postcast',
    templateUrl: './postcast.component.html',
    styleUrls: ['./postcast.component.scss'],
    encapsulation: ViewEncapsulation.None
})
export class PostcastComponent {
}